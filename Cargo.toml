[package]
name = "dicetest"
version = "0.1.0"
authors = ["Jakob Schikowski"]
description = "Framework for writing tests with pseudorandom generated test data."
edition = "2018"

[dependencies]
rand = "0.5"

[features]
default = ["hints", "stats"]
hints = []
stats = []
